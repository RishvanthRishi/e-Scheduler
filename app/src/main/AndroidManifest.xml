<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.girish.e_scheduler">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AuthoirtyActivity"
            android:label="e-Scheduler" />
        <activity android:name=".Pankaj" />
        <activity android:name=".Tapaswi" />
        <activity android:name=".Anupam"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".Rajendra" />
        <activity android:name=".Wilfred"> </activity>
    </application>

</manifest>
